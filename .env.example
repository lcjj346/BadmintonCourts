DATABASE_URL="postgresql://postgres:postgres@localhost:5433/badmintonsg"
DIRECT_URL="postgresql://postgres:postgres@localhost:5433/badmintonsg"
IP_HASH_SALT="dev-only-salt-change-in-prod"
# Optional: Sentry error reporting (client + server). Leave unset locally —
# the SDK is fully inert without it. Set in Vercel (Production scope) with the
# DSN from your sentry.io project settings.
NEXT_PUBLIC_SENTRY_DSN=""
